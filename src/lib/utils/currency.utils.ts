/**
 * @deprecated Import from `@/lib/money/money` instead.
 * Re-exports kept so existing imports keep working.
 */
export {
  formatCurrencyAmount,
  formatCurrencyDisplay,
  parseCurrencyAmount,
  parseCurrencyToMinor,
  majorToMinor,
  minorToMajor,
  toMinorInt,
  MONEY_SCALE,
  MONEY_FACTOR,
} from "@/lib/money/money";
